/**
 * Shared so a term marked in the results list keeps the same colour when the
 * reader lands in the transcript — the mark is how they recognise what they
 * clicked, and a different colour there would read as a different thing.
 */

/** Entity mentions, and the search term in results. */
export const ENTITY_HIGHLIGHT_COLOR = '#fde047';

/** The term typed into the "filter these results" box. */
export const FILTER_HIGHLIGHT_COLOR = '#a5d8ff';
